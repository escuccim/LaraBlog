language: php

php:
  - 7.0
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
  - php artisan make:auth
  - php artisan migrate

script: phpunit
